apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: agent-pod-operator
  namespace: agents-ns
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["create", "list"]
  - apiGroups: [""]
    resources: ["configmaps", "secrets"]
    verbs: ["create", "delete"]
