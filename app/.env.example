# Path to the YAML config file. Optional, defaults to ./config.yaml.
A3S_CONFIG_PATH=

# Service account token for the remote Kubernetes cluster.
# Required when deployment mode resolves to `remote`.
K8S_SERVICE_ACCOUNT_TOKEN=
